node_modules/
dist/
.env

# User-managed state — generated by `wife init` and edited via the TUI/CLI.
data/config.json
data/schedule.json
data/messages.log

# Legacy log paths (kept for safety if anyone still has them)
messages.log
cron-errors.log
ofelia.ini